mixin videoBlock(video = {})
    .videoBlock
        a(href=routes.videosDetail(video.id))    
            video.videoBlock__thumpnail(controls=true, src=video.videoFile)
            h4.videoBlock__title=video.title
            h4.videoBlock__views=video.views